import Background from "./Background/Background";

export default () => {
	return (
		<section class="z-0 min-h-screen w-full overflow-hidden relative">
			<Background></Background>
		</section>
	);
};
