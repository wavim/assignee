export default (props: { class?: string; ref?: any }) => (
	<div></div>
	// <button
	// 	ref={props.ref}
	// 	class={`inset-ring-p-light group relative flex h-max w-max cursor-pointer items-center overflow-hidden rounded-full border-2 border-none bg-transparent inset-ring-2 ${props.class ?? ""}`}
	// >
	// 	<div class="bg-p-light absolute top-0 right-0 left-0 h-full origin-top scale-y-0 transition-transform duration-200 ease-out group-hover:origin-bottom group-hover:scale-y-100"></div>
	// 	<span class="bg-p-light font-p px-8 py-6 text-3xl font-medium mix-blend-difference">
	// 	</span>
	// </button>
);
